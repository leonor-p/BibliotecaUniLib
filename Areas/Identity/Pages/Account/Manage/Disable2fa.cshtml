@page
@model Disable2faModel
@{
    ViewData["Title"] = "Desativar autenticação de dois fatores (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Esta ação apenas desativa a 2FA.</strong>
    </p>
    <p>
        Desativar a 2FA não altera as chaves usadas nas aplicações de autenticação. Se pretende alterar a chave
        usada numa aplicação de autenticação, deve <a asp-page="./ResetAuthenticator">repor as suas chaves de autenticação.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn-submit" type="submit">Desativar 2FA</button>
    </form>
</div>
